import fixSidebarHeight from '../../../utils/fix-sidebar-height';
import { module, test } from 'qunit';

module('fixSidebarHeight');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fixSidebarHeight();
  assert.ok(result);
});
