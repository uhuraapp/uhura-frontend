import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:login');

// Replace this with your real tests.
test('it exists', function(assert) {
  let view = this.subject();
  assert.ok(view);
});
