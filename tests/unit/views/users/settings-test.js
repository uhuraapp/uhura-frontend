import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:users/settings');

// Replace this with your real tests.
test('it exists', function(assert) {
  let view = this.subject();
  assert.ok(view);
});
