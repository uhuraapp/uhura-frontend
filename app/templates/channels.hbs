<h3>{{t 'titles.categories'}} </h3>
<ul id="categories">
  {{#each category in categories}}
  <li class="">
    <a class="">
      <img {{bind-attr src=category.thumb_url}} />
      <h3>{{category.name}}</h3>
    </a>
  </li>
  {{/each}}
</ul>
